{
    "$schema": "https://schema.railpack.com",
    "steps": {
        "install": {
            "commands": [
                "npm install -g pnpm",
                "pnpm install --no-frozen-lockfile"
            ]
        }
    }
}